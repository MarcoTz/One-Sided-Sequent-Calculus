workspace:
  extraPackages: {}
  packageSet:
    registry: 50.11.0

package:
  name: one-sided-sc
  dependencies:
    - one-sided-web-app
    - one-sided-examples
  bundle:
    module: Main
    outfile: "./web-app/index.js"
    platform: browser
  test:
    main: Test.Main
    dependencies:
      - prelude
      - one-sided-defs
